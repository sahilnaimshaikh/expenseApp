# Expense Tracker

Offline-first personal expense tracker for Android, built with Flutter, Isar (local storage), and Riverpod. No backend, no cloud, no user accounts — all data stays on-device.

See the full requirements at [aidlc-docs/inception/requirements/requirements.md](aidlc-docs/inception/requirements/requirements.md) and the original PRD at [aidlc-docs/inception/requirements/prd-expense-tracker.md](aidlc-docs/inception/requirements/prd-expense-tracker.md).

## Getting Started

### Prerequisites
- Flutter SDK (stable channel, Dart >=3.4.0) — install via https://flutter.dev/docs/get-started/install
- Android Studio or VS Code with the Flutter/Dart plugins
- An Android emulator or physical device (Android 6.0+ recommended for `flutter_local_notifications` compatibility)

### Setup
```bash
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs   # generates Isar's *.g.dart files
flutter run
```

**Note**: This repository was authored in an environment without the Flutter/Dart SDK installed, so `*.g.dart` files (generated by `isar_generator`/`build_runner` from the `@collection` classes in `lib/features/*/domain/models/`) are **not** checked in and must be generated locally with the command above before the app will compile. See [aidlc-docs/construction/build-and-test/build-instructions.md](aidlc-docs/construction/build-and-test/build-instructions.md) for full details.

## Project Structure

Feature-first organization, one folder per unit of work (see [aidlc-docs/inception/application-design/unit-of-work.md](aidlc-docs/inception/application-design/unit-of-work.md)):

```
lib/
  features/
    core_data/        # Isar setup, all repositories, shared enums (Unit 1)
    categories/        # Category management (Unit 1)
    onboarding/         # First-launch wizard (Unit 2)
    expense_management/ # Expense CRUD, list screen (Unit 2, extended by Unit 5)
    budgeting/           # Budget calculation, thresholds (Unit 3)
    notifications/       # Local notification dispatch (Unit 3)
    dashboard/           # Financial overview screen (Unit 4)
    search_filter/       # Search/filter/sort (Unit 5)
    reporting/           # Analytics/reports (Unit 5)
    backup_export/       # Backup, restore, export (Unit 6)
    settings/             # App settings, custom categories UI (Unit 6)
  main.dart
```

Each feature folder contains `data/`, `domain/`, and `presentation/` sub-layers per the hybrid layered architecture in [aidlc-docs/inception/application-design/application-design.md](aidlc-docs/inception/application-design/application-design.md).

## Build and Test

See [aidlc-docs/construction/build-and-test/](aidlc-docs/construction/build-and-test/) for full build, unit test, integration test, and property-based test instructions.

## Development Process

This project was built using the AI-DLC (AI-Driven Development Life Cycle) workflow. Full requirements, user stories, application design, and per-unit design artifacts are in [aidlc-docs/](aidlc-docs/). Progress tracking is in [aidlc-docs/aidlc-state.md](aidlc-docs/aidlc-state.md).

## Contribute

This is a personal-use project built end-to-end via an AI-assisted workflow. See [aidlc-docs/aidlc-state.md](aidlc-docs/aidlc-state.md) for current build status before making changes.
# expenseApp
